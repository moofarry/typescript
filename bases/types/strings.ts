(() => {
  const batman: string = "batman";
  const superman: string = "superman";
  const flash: string = `flash`;



  console.log(batman, superman, flash);

  console.log(`${batman.toUpperCase()} ${superman.toUpperCase()} ${flash}`);
  
  
})();
